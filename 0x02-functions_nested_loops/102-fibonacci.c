#include <stdio.h>

/**
* main - Prints sum of multiples of 3 and 5
* Return: 0 for success
*/

int main(void)
{
	int count;
	long int current = 1, next = 2;
	long int temp;

	printf("%lu, ", i);
	for (count = 2; count <= 50; count++)
	{
		if (count == 0)
			printf("%lu\n", j);
		else
			printf"%lu, ", j);

		temp = next;
		next += current;
		current = temp;
	}

	return (0);
}
